
{
  "name": "coeur-source-driver",
  "version": "1.0.0",
  "description": "Coeur Scripts",
  "author": "Jan",
  "private": true,
  "scripts": {
    "pre_build":  "cp ../readme.md _imported_readme.md.erb",

    "server":     "npm run pre_build; bundle exec middleman server --port 4567",
    "build":      "npm run pre_build; bundle exec middleman build ",
    "deploy":     "npm run pre_build; bundle exec middleman deploy ",
    "github":     "npm run pre_build; bundle exec middleman build ; bundle exec middleman deploy",
    
    "stage":      "npm run pre_build; bundle exec middleman build ; cd mm_build ; surge . -d stylecoeur_stage.surge.sh",
    "publish":    "npm run pre_build; bundle exec middleman build ; cd mm_build ; surge . -d stylecoeur.surge.sh"
  }
}
